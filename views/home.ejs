<%- include('partials/header'); -%>

<h1>Home</h1>

<p><%= homeContent %></p>

<% postList.forEach(post => { %>
        <h1><%= post.title %></h1>
        <p class="min-post"> <%= post.post.substring(0,100) %> <a href="/posts/<%= post.title %>">Read More</a> </p>
<% }); %> 

<%- include('partials/footer'); -%>
